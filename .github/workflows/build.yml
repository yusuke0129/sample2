name: build
on:
  pull_request:
# testaaaaaaaaaaaaazzzz
jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./
    steps:
      - uses: actions/checkout@v3

      - uses: docker/setup-buildx-action@v2

      - uses: actions/cache@v3
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-{{ hashFiles('Gemfile.lock') }}{{ hashFiles('docker-compose.yml') }}

      - uses: docker/build-push-action@v2
        with:
          context: .
          tags: app:latest
          load: true
          cache-from: type=gha
          cache-to: type=gha,mode=max    
      #   with:
      #     submodules: recursive
      # - uses: docker/setup-buildx-action@v2
      # - uses: docker/build-push-action@v4
      #   with:
      #     context: .
      #     tags: app:latest
      #     load: true
      #     cache-from: type=gha
      #     cache-to: type=gha,mode=max

      - run: docker-compose -f docker-compose.yml up -d

      - run: docker-compose run --rm app bundle install

      - run: docker-compose run --rm app rails db:create RAILS_ENV=test

      - run: docker-compose run --rm app rails db:migrate RAILS_ENV=test
      - run: docker-compose run --rm app bundle exec rspec


  # rspec:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   defaults:
  #     run:
  #       working-directory: ./
  #   steps:
  #     - uses: actions/checkout@v2

  #     - uses: satackey/action-docker-layer-caching@v0.0.11
  #       with:
  #         key: test
  #       continue-on-error: true

  #     - run: docker-compose run --rm app rails db:create RAILS_ENV=test

  #     - run: docker-compose run --rm app rails db:migrate RAILS_ENV=test
  #     - run: docker-compose run --rm app bundle exec rspec
  
  # rubocop:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   defaults:
  #     run:
  #       working-directory: ./
  #   steps:
  #     - uses: actions/checkout@v2

  #     - uses: satackey/action-docker-layer-caching@v0.0.11
  #       continue-on-error: true

  #     - run: docker-compose run --rm app bundle exec rubocop